import * as React from 'react';
import * as ReactDOM from 'react-dom';

import { App } from './components/App';

const Root = document.getElementById('root');
ReactDOM.render(<App name="you" />, Root);
